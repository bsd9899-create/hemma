/**
 * الحركة في هِمّة: قليلة، قصيرة، ولها سبب.
 *
 * ما تفعله الحركة هنا شيء واحد: تربط الهيكل العظمي بالمحتوى الذي حلّ
 * محلّه. بدونها يُستبدل الهيكل بالبيانات في إطار واحد، فتُقرأ الشاشة
 * كأنها قفزت. ولا حركة على التنقّل ولا على الأرقام: عدّاد يتصاعد يؤخّر
 * قراءة الرقم بلا فائدة، وهو خدعة بصرية لا معلومة.
 */

/** مدد الحركة بالمللي ثانية. */
export const duration = {
  /** ظهور المحتوى بعد التحميل. أقصر من هذا يُقرأ كوميض، وأطول يُقرأ كبطء. */
  enter: 220,
  /** ردّ فعل اللمس. */
  press: 90,
} as const;

/** إزاحة الظهور — قصيرة عمدًا: إيحاء بالاستقرار لا انزلاق كامل. */
export const ENTER_OFFSET = 8;
